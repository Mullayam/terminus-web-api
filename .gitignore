# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
build
dist-ssr
*.local
.env
# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Embedded RocksDB store (local dev; production uses a Docker volume)
.terminus-store/

# Codeium language-server binary + runtime dirs. The ~170 MB binary is
# downloaded on demand and cached; never commit it. Bake it into the image
# via CODEIUM_BINARY_PATH for production instead.
codeium-bin/
.codeium/
**/language_server
**/language_server.exe
*.gz
